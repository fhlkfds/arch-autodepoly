# For provisioning the machine you are sitting at, rather than the workstation
# over SSH. Same play, same roles; only the connection differs, which matters
# for the roles that close the SSH port behind themselves.
#   ansible-playbook -i inventory/local.ini site.yml --ask-become-pass
[arch_workstation]
localhost ansible_connection=local ansible_user=liam
